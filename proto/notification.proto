syntax = "proto3";

option go_package = "./notifications";

message ForgotPasswordRequest {
    string token = 1;
}

message ForgotPasswordResponse {
    string message = 1;
}

service ForgotPasswordService {
    rpc SendEmailForgotPassword(ForgotPasswordRequest) returns(ForgotPasswordResponse) {}
}
